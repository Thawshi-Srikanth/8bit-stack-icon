{
  "name": "8bit-stack-icon",
  "version": "0.1.0",
  "description": "A growing library of 8-bit tech stack icons.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "generate": "bun run scripts/generate.ts",
    "compile": "tsc",
    "build": "bun run generate && bun run compile",
    "prepublishOnly": "bun run build"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^24.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.2.3",
    "react-dom": "^19.2.3",
    "typescript": "^5.0.0"
  },
  "peerDependencies": {
    "react": ">=16.8.0"
  },
  "files": [
    "dist"
  ]
}